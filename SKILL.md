---
name: figma-text-naming
description: 扫描指定 Figma 页面中顶层 `预览图/*` Frame 内含动态占位符的文本，根据 Figma 可观察的语义上下文生成简短稳定的业务名称，识别重复和待确认项；默认仅预览，用户明确要求后写回并回读验证。
---

# Figma 动态文本命名

只负责定位一个 Figma 页面，扫描其中的动态文本，依据 Figma 可观察上下文进行语义命名和去重，并在用户明确执行后写入、回读。默认只读。

## 按阶段加载 Figma 规则

调用 `use_figma` 前按宿主要求加载并遵循 `figma-use` Skill，只读取当前阶段需要的操作规则：

1. **扫描与预览**：页面定位、节点查找、名称/类型/文本/层级/可见性/实例状态读取，以及分页和截断处理。
2. **执行命名**：仅在用户明确要求时，额外读取节点改名、批量限制和按节点标识回读的规则。

后续阶段能力缺失不得阻止只读预览。不得猜测工具参数或使用未经说明的写入方式。

## 定位目标页面

按用户提供的信息定位一个 Figma 页面，支持以下入口：

1. 用户提供的 Figma 页面 URL，或可向上解析到所属页面的节点 URL；
2. 用户当前选中的 Figma 页面；
3. 用户明确指定且在文件内唯一匹配的页面名称。

只要能唯一确定一个页面就继续扫描。若 URL、当前选择或页面名称指向不同页面，或同名页面无法消歧，则停止并要求用户明确目标页面。

## 扫描范围

1. 只进入目标页面的直接子级中类型为 `FRAME` 且名称匹配 `^预览图\/[^/]+$` 的节点。
2. 扫描这些 Frame 内的全部后代 `TEXT` 节点，包括隐藏节点。
3. 按 [references/naming-rules.md](references/naming-rules.md) 的精确表达式识别含 `xx`、`XX` 等动态占位符的候选项。
4. 当前名称合法的候选仍需扫描，以复核语义和去重关系。
5. 实例继承文本列入 `skippedInstances`，不直接改名；记录其组件上下文，默认预览不展开。

## 两阶段扫描

### 第一阶段：候选发现

在已定位页面中合并 Frame 定位和候选扫描。每个候选只返回：

```json
{
  "nodeId": "",
  "name": "",
  "characters": "",
  "parentPath": [],
  "frameId": "",
  "frameName": "",
  "visible": true,
  "instanceState": "editable | inherited | unknown"
}
```

`parentPath` 只包含从目标 Frame 到节点的精简名称路径。第一阶段不返回完整样式、全部祖先属性、组件对象、布局属性、图片或导出信息。

候选较多时使用稳定游标分页，按 Frame 页面顺序、节点层级/视觉顺序和 `nodeId` 合并。先读取 [references/naming-rules.md](references/naming-rules.md)；需要比较复用关系时再读 [references/deduplication.md](references/deduplication.md)，需要选择业务词时再读 [references/text-key-catalog.md](references/text-key-catalog.md)。

### 第二阶段：一次批量补查

汇总全部实际歧义，只发起一次批量上下文查询。按需读取直接语义祖先、相邻文本、组件名、变体名和会影响最终展示的局部样式。不得重新返回全部候选或重复查询单个节点。

以下情况才补查：业务域不明、需要组件或变体判断用途、需要区分不同文字的语义、相同文本需要比较局部样式、实例状态不明，或当前名称与可见上下文冲突。同一规范化文本出现多次时，合并前必须比较会影响最终展示的局部样式；无法完成必要的样式比较时标为 `confirm`。第二阶段后仍缺少 Figma 正向证据的节点也标为 `confirm`。

## 分类、命名与去重

- `exact`：规范化文字、占位符结构和有意义的局部样式一致，共用一个无编号名称。
- `near`：规范化文字和占位符结构相同，但有意义的局部样式不同；使用共同语义加稳定的两位编号。
- `unique`：文字或占位符结构不同，根据各自文字语义独立命名。
- `confirm`：完成一次补查后，Figma 证据仍不足或互相冲突。

格式固定为 `<business-domain>/<semantic-key>`，路径段使用小写英文 kebab-case。语义键一般使用 1 至 3 个简短英文词。当前名称只有在格式合法、足够简短、与可观察语义一致且复用关系正确时才保留；不一致时进入 `confirm`，不得仅因它是历史名称而保留。

按以下优先级处理：文字和样式相同，共用无编号名称；文字相同但样式不同，使用共同语义加 `-01`、`-02`；文字不同，根据各自文字语义命名。去重以当前目标 Figma 页面为边界：同页不同 `预览图/*` Frame 可以共同去重，不跨页面自动分组。页面、Frame、组件区域、158/388 等区域标识、节点位置和节点标识只用于查找、命名取证、排序和写入，不得单独拆分相同文字，也不参与去重签名。具体规则见 [references/deduplication.md](references/deduplication.md)。

## 计划结构

```json
{
  "figmaPageNodeId": "",
  "figmaPageName": "",
  "scopeFrameIds": [],
  "renames": [],
  "nearDuplicates": [],
  "skippedInstances": [],
  "conflicts": []
}
```

`figmaPageNodeId` 只用于定位目标页面，不参与命名或去重签名。默认不向用户展示该字段；仅在同名页面、定位冲突或用户明确要求技术明细时展示。

## 默认预览

严格按以下结构输出摘要、三列表格、必要的异常汇总和结束语。每个建议名称一行；完全重复组不展开成员。

```text
扫描到 26 个候选文本，建议使用 15 个名称。
本次仅预览，未修改 Figma。

| 建议名称 | Figma 文本 | 中文含义 |
| --- | --- | --- |
| coupon/countdown | {{}}时{{}}分 | 优惠券倒计时 |
| reward/badge-rule | استلم {{}} هدية إضافية ... المستوى {{}} | 徽章解锁条件 |
| reward/gift-card-name | بطاقة الهدية\n{{}} | 礼物卡名称 |
| chest/round | الجولة الحالية: {{}} | 当前轮次 |
| chest/round-score | {{}} | 当前轮次积分 |
| txt/lottery | عدد مرات السحب الحالية: {{}} مرة | 当前抽奖次数 |
| reward/outfit-name | أزياء {{}} | 服饰名称 |
| txt/had-send | تم الإرسال {{}} مرة ولم يتم إطلاق مكافأة 5 نجوم | 已发送次数 |
| txt/times | {{}} ضعف | 奖励倍数 |
| txt/treasury | قيمة الثروة الحالية: {{}}/100 | 当前财富值 |
| txt/progress | قيمة الطاقة الخاصة بي حالياً: {{}}/100 | 当前能量值 |
| ring/proposal | أرسل 30 هدية من “{{}}” أو عندما تصل... | 求婚条件 |
| ring/open-count | عدد مرات الفتح الحالية: {{}} | 戒指盒开启次数 |
| member/renew-chance | تجديد العضوية {{}} شهر = {{}} فرصة... | 续费获得开箱机会 |
| member/open-chance | عدد الفرص الفتح: {{}} | 当前开箱机会 |

待确认：
- `reward/badge-rule`：当前只能确认是徽章条件，具体业务称谓不明确。

失败：
- 节点 `12:34`：回读失败，未确认改名结果。

回复“执行命名”后写入 Figma 并回读验证。
```

表格中的 Figma 文本先把动态占位符统一写成 `{{}}`；换行写成字面量 `\n`，过长文本可用 `...` 截断。文字和样式相同的重复组只显示一个无编号名称，如上表的 `ring/open-count`。

文字相同但有意义的局部样式不同时，显示共同语义加编号，不在默认表格中展示具体颜色或样式值：

```text
| 建议名称 | Figma 文本 | 中文含义 |
| --- | --- | --- |
| ring/open-count-01 | عدد مرات الفتح الحالية: {{}} | 戒指盒开启次数（样式 1） |
| ring/open-count-02 | عدد مرات الفتح الحالية: {{}} | 戒指盒开启次数（样式 2） |
```

默认不显示 Pagecenter、节点数、状态、置信度、处理方式、判断依据、具体颜色、样式代码、重复组成员或内部签名。只有用户需要确认样式差异时，才在表格后说明相关差异。

仅在确有对应项时，才在表格后显示 `待确认` 或 `失败`。`待确认` 只简述仍不明确的业务含义；`失败` 只列节点标识和失败结果。没有对应异常时不得展示空标题。用户明确要求技术明细时，才补充相关节点的当前名称、Figma 上下文和具体差异。

## 执行命名

仅在用户明确要求“执行命名”或同义指令后进入；跳过 `confirm` 和实例继承节点：

1. 用预览快照复核目标仍在范围内，文本、旧名称和实例状态未变化。
2. 按预览建议改名，不静默重新生成；工具有批量限制时每批最多 10 个节点，只改名称，不加载字体。
3. 每批后按节点标识回读。名称等于目标名称且仍通过规则校验才记为成功。
4. 分别报告成功、跳过、待确认、失败和未执行；回读不可用时标为 `executedUnverified`。写入失败后停止后续批次，不自动覆盖其他改动。

## 稳定性

- 再次扫描不得因折叠、隐藏、选中状态、扫描顺序或新增无关节点改变已有合法名称。
- 首次分配需要排序时可使用 Frame 页面顺序、层级/视觉顺序和节点标识；这些信息不得进入名称或去重签名。
