---
name: figma-text-naming
description: 识别 Figma 中含 `xx`、`XX` 等占位符的动态文字与特效文字，生成简洁稳定的业务 Key 和完整中文含义，并默认写回对应文字图层名称；用户明确要求只读时不写入。仅用于用户提供 Figma Design 链接并提到“动态文字命名”“特效文字命名”或“特效文本”时；其他措辞、单独的 Figma 链接以及一般的设计查看、实现、修改或普通文字编辑请求不得触发。
---

# Figma 动态文本命名

根据用户提供的 Figma Design 链接完成动态文本识别和命名。调用 Figma `use_figma` 前，先加载并遵循 `figma-use` Skill。

## 处理

1. 以链接指向的文件、页面或节点为范围，查找包含动态占位符的文本节点。
2. 只结合原文和链接范围内必要的 Figma 上下文理解字段含义；上下文不足时保留待确认，不猜测。
3. 命名前完整读取 [references/naming-standard.md](references/naming-standard.md)，严格按其中的权威规则复核或生成名称。
4. 去重只用于合并完全相同的候选：原文、占位符结构、文字样式和业务语义一致时归为一组；同组复用一个业务 Key，预览合并展示，写回时处理组内所有可写文本节点。
5. 上述三个触发词之一触发 Skill 后，除非用户明确要求只读、预览或不要修改，否则无需再次询问或等待“写入”“写回”等提示词，直接把业务 Key 写入对应文字图层的 `node.name` 并回读确认。
6. 已有且符合规范、语义准确的图层名称保持不变；不符合规范或语义不准确时再修改。

## 命名

[references/naming-standard.md](references/naming-standard.md) 是候选识别、业务 Key 和中文含义的唯一命名事实源；不要凭记忆重写规范。

## 输入与输出

- 只接受 Figma Design 链接作为设计输入，并结合用户的自然语言要求执行；不要求或读取飞书、需求文档、代码仓库、额外配置或固定表单。
- 不扩大链接限定的范围；链接无法读取时只说明实际问题。
- 结果只包含动态文本、中文含义和建议名称；写回时说明图层名称的实际修改结果。
- 只列出确实无法判断的项目及简短原因。除非用户要求，不输出内部账本、固定分类、统计指标、置信度、样式签名、节点 ID、执行日志或回滚清单。
- 没有候选时，用一句话说明即可。

## 边界

- 只处理动态文本命名和对应文字图层的 `node.name`，不修改文字内容、位置、尺寸、样式、组件关系或其他 Figma 数据。
- Figma Design 链接与“动态文字命名”“特效文字命名”或“特效文本”之一共同授权写回图层名称；其他措辞和单独链接不触发本 Skill，用户明确要求只读、预览或不要修改时禁止写入。
