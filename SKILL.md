---
name: figma-text-naming
description: 识别 Figma 中含 `xx`、`XX` 等占位符，或在完整预览图中出现但在对应切图中缺失的动态文字与特效文字；在文案命名阶段按重复原文、业务字段和完整 HTML 一次完成去重，按 `文案/${business-domain}/${semantic-key}` 生成、复核和写回图层名称，并可上传 Page Center。用于用户提供 Figma Design 链接并提到“动态文字命名”“特效文字命名”“特效文本”“文案去重”或“重复文案命名”时；其他措辞、单独的 Figma 链接以及一般设计查看、实现、修改或普通文字编辑请求不得触发。
---

# Figma 动态文本命名

## 执行流程

1. 调用 Figma `use_figma` 前，加载并遵循 `figma-use` Skill。
2. 完整读取 [references/dynamic-text-naming-rules.md](references/dynamic-text-naming-rules.md)。运行时不得读取飞书；该本地快照是唯一命名事实源。
3. 将用户链接限定的文件、页面或节点作为唯一扫描范围。先读取页面结构、Text `characters`、边界和素材关系，再根据实际证据缺口有针对性地扩展；不得越出链接范围。
4. 按规则快照和 [scripts/dynamic-text-naming.mjs](scripts/dynamic-text-naming.mjs) 的候选合同识别占位符候选。不得用当前图层名称代替 `characters` 判断。
5. 图片关系明确时直接比较完整预览图与切图；关系不明确时，结合结构、命名、位置、标注和视觉特征寻找可能的对应关系，再核实版本、语言、界面状态、共同覆盖区域和唯一 Text 节点映射。
6. 对候选收集所属一级板块、板块业务域、字段语义和平行字段证据，检查当前名称及名称冲突。优先复用同一板块、共享祖先和已读取素材的证据；局部证据不足时逐层扩大范围，仍不足时标记 `confirm`，不得猜测。
7. 在生成名称前，按逐字相同的 `node.characters` 找出重复文案组。逐组核实成员是否为同一业务字段；仅原文相同但业务含义不同的成员不得合并。
8. 对已确认属于同一业务字段的重复文案组读取全部成员的 Figma styled text segments；用户要求上传 Page Center 时，对全部候选读取。按颜色、字号、字重、行高的固定顺序生成规范 HTML，字号按 `100px = 1rem` 转换，`X` / `XX` / `xx` 等占位符转换为 `{{}}`，多种样式生成多个连续 `<span>`。需要比较或上传但无法取得样式时，将该项标记为 `confirm` 并跳过写入与上传。
9. 在同一次命名分组中按“逐字相同原文 + 同一业务字段 + 完整 HTML”确定最终名称。三项均相同的节点复用一个名称；原文与字段相同但 HTML 不同的组立即使用稳定场景限定生成不同名称，无论本次是否上传 Page Center。不得选择其中一套样式覆盖，也不得用字号、节点 ID 或无语义数字区分；无法确定稳定场景限定时标记 `confirm`。
10. 对当前名称和拟写名称运行 `node '<本 Skill 目录>/scripts/validate-dynamic-text-name.mjs' '<name>'`。格式通过后仍须完成业务域、字段语义、重复文案分组和名称冲突检查。为每个文本节点确定且仅确定一个结果：`rename`、`keep`、`skip` 或 `confirm`。
11. 用户明确要求只读、预览或不要修改时停止在结果预览。否则只把 `rename` 的最终名称写入对应文字图层 `node.name`，再回读每个已写节点确认实际名称；`keep`、`skip` 和 `confirm` 不写入。
12. 用户要求上传 Page Center 时，直接使用第 9 步已经确定的最终名称和 HTML 建立完整的 `key -> HTML` 映射。映射只作为安全校验，不得在上传阶段重新分组、重新命名或覆盖冲突值；校验通过后上传全部 `rename` 和 `keep` 项并逐项回读。Page Center key 去掉 `文案/` 前缀，`value` 必须使用生成的 HTML，不得直接上传 `characters`。

## 搜索与收敛原则

- 优先读取信息密度高、成本低的页面结构、Text `characters`、边界和素材关系，再围绕候选补充证据。
- 优先复用同一板块、共享祖先和已读取素材的证据，避免重复获取同一上下文。
- 图片关系明确时直接比较；关系不明确时可以结合结构、命名、位置、标注和视觉特征寻找并验证对应关系。
- 局部证据不足时允许逐层扩大范围。样式、完整节点树或更广范围的信息，在确实有助于解决当前证据缺口时再读取。
- 搜索方式可以适应新结构，读写优先批量处理；工具条件不适合时允许采用其他可靠方式，但不得越出用户限定范围、猜测名称、跳过必需校验或省略写后回读。

## 读取合同

- 只接受 Figma Design 链接和用户自然语言要求作为任务输入。
- 只读取链接范围内完成结构归属和语义判断所需的 Figma 内容。
- 运行时不读取飞书、需求文档、代码仓库、外部配置或其他资料作为命名证据。
- 当前名称即使格式正确，也必须复核一级板块、业务域、字段语义和冲突；只有四项全部正确才能 `keep`。
- 预览图与切图只用于发现候选；最终“动态文本”原文必须来自唯一匹配的 Text 节点 `characters`，不得从图片 OCR 结果代替或改写。
- styled text segments 在重复文案命名时用于一次性生成最终分组，上传 Page Center 时还用于生成全部待上传 HTML；样式不得用于推断业务域或字段语义。

## 写入合同

- Figma Design 链接与“动态文字命名”“特效文字命名”“特效文本”“文案去重”或“重复文案命名”之一共同授权默认写回；用户明确要求只读、预览或不要修改时禁止写入。
- 只修改 `rename` 项对应文字图层的 `node.name`。
- 不修改候选文字的 `characters`、样式、位置、尺寸、可见性、一级板块结构、组件关系或其他 Figma 数据。
- 不得向同一个 Page Center key 写入两份不同 HTML；上传前映射检查只验证命名阶段的结果，不得触发第二轮去重或改名。
- Figma 连接器不可用、无编辑权限或写后回读不一致时，只报告实际状态，不声称写入成功。

## 输出合同

- 用户明确要求只读、预览或不要修改时，输出候选明细：动态文本、完整中文含义、结果和名称；`confirm` 只给简短待确认原因。明细默认使用编号列表，不使用 Markdown 表格；只有用户明确要求表格时才使用表格。
- 预览中的“动态文本”必须逐字复制 `node.characters`，保留原语言、标点、大小写、空白、换行和占位符写法，不得翻译、概括、纠错或规范化。相同业务字段可合并预览，但必须逐项列出组内每个不同的原文，不得用中文业务标签代替。
- 执行写回后，最终回执禁止重复输出全量候选明细或全量表格，只输出一行摘要：`命名完成：共扫描 N 个文本，已正确命名 M 个，跳过 S 个，待确认 C 个，失败 F 个。` `已正确命名` 包含写回并回读一致的 `rename` 和复核正确的 `keep`；`跳过` 对应 `skip`；`待确认` 对应 `confirm`；只有写入失败、回读不一致或其他执行错误才计入 `失败`。
- 最终回执只在 `待确认` 或 `失败` 大于零时逐项列出对应动态文本和简短原因；不列出成功项、跳过项、节点 ID、内部账本、置信度、样式签名或执行日志，除非用户明确要求。
- 上传 PC 时，在命名摘要后另加一行 `PC 上传：成功 U 个，失败 P 个。`；只逐项列出上传失败项。样式冲突导致的 `confirm` 仍列在待确认项中，并说明稳定场景限定无法确定；不得把纯文本报告为 HTML 上传成功。
- 没有候选时用一句话说明，不输出空表格。
