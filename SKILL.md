---
name: figma-text-naming
description: 扫描指定 Figma 页面中顶层 `预览图/*` Frame 内含动态占位符的文本，根据 Figma 可观察的语义上下文生成简短稳定的业务名称，识别重复和待确认项；默认仅预览，用户明确要求后写回并回读验证。
---

# Figma 动态文本命名

只负责定位一个 Figma 页面，扫描其中的动态文本，依据 Figma 可观察上下文进行语义命名和去重，并在用户明确执行后写入、回读。默认只读。

## 按阶段加载 Figma 规则

调用 `use_figma` 前按宿主要求加载并遵循 `figma-use` Skill，只读取当前阶段需要的操作规则：

1. **扫描与预览**：页面定位、节点查找、名称/类型/文本/层级/可见性/实例状态读取，以及分页和截断处理。
2. **执行命名**：仅在用户明确要求时，额外读取节点改名、批量限制和按节点标识回读的规则。

后续阶段能力缺失不得阻止只读预览。不得猜测工具参数或使用未经说明的写入方式。

## 定位目标页面

按用户提供的信息定位一个 Figma 页面，支持以下入口：

1. 用户提供的 Figma 页面 URL，或可向上解析到所属页面的节点 URL；
2. 用户当前选中的 Figma 页面；
3. 用户明确指定且在文件内唯一匹配的页面名称。

只要能唯一确定一个页面就继续扫描。若 URL、当前选择或页面名称指向不同页面，或同名页面无法消歧，则停止并要求用户明确目标页面。

## 扫描范围

1. 只进入目标页面的直接子级中类型为 `FRAME` 且名称匹配 `^预览图\/[^/]+$` 的节点。
2. 扫描这些 Frame 内的全部后代 `TEXT` 节点，包括隐藏节点。
3. 按 [references/naming-rules.md](references/naming-rules.md) 的精确表达式识别含 `xx`、`XX` 等动态占位符的候选项。
4. 当前名称合法的候选仍需扫描，以复核语义和去重关系。
5. 实例继承文本列入 `skippedInstances`，不直接改名；报告其组件上下文。

## 两阶段扫描

### 第一阶段：候选发现

在已定位页面中合并 Frame 定位和候选扫描。每个候选只返回：

```json
{
  "nodeId": "",
  "name": "",
  "characters": "",
  "parentPath": [],
  "frameId": "",
  "frameName": "",
  "visible": true,
  "instanceState": "editable | inherited | unknown"
}
```

`parentPath` 只包含从目标 Frame 到节点的精简名称路径。第一阶段不返回完整样式、全部祖先属性、组件对象、布局属性、图片或导出信息。

候选较多时使用稳定游标分页，按 Frame 页面顺序、节点层级/视觉顺序和 `nodeId` 合并。先读取 [references/naming-rules.md](references/naming-rules.md)；需要比较复用关系时再读 [references/deduplication.md](references/deduplication.md)，需要选择业务词时再读 [references/text-key-catalog.md](references/text-key-catalog.md)。

### 第二阶段：一次批量补查

汇总全部实际歧义，只发起一次批量上下文查询。按需读取直接语义祖先、相邻文本、组件名、变体名和可能改变文本含义的局部样式。不得重新返回全部候选或重复查询单个节点。

以下情况才补查：业务域不明、相同文本可能属于不同对象、需要组件或变体判断用途、需要区分同类项、样式可能改变含义、实例状态不明，或当前名称与可见上下文冲突。第二阶段后仍缺少 Figma 正向证据的节点标为 `confirm`。

## 分类、命名与去重

- `exact`：文本结构、Figma 语义、用途和必要样式一致，共用一个无编号名称。
- `near`：文本相同或相近，但语义、状态、所属对象或有意义样式不同；分别命名或使用稳定编号。
- `unique`：没有重复或近似候选，独立命名。
- `confirm`：完成一次补查后，Figma 证据仍不足或互相冲突。

格式固定为 `<business-domain>/<semantic-key>`，路径段使用小写英文 kebab-case。语义键一般使用 1 至 3 个简短英文词。当前名称只有在格式合法、足够简短、与可观察语义一致且复用关系正确时才保留；不一致时进入 `confirm`，不得仅因它是历史名称而保留。

去重以当前目标 Figma 页面为边界：同页不同 `预览图/*` Frame 可以共同去重，不跨页面自动分组。页面、Frame 和节点的定位信息只用于查找、排序和写入，不参与语义判断或去重签名。具体规则见 [references/deduplication.md](references/deduplication.md)。

## 计划结构

```json
{
  "figmaPageNodeId": "",
  "figmaPageName": "",
  "scopeFrameIds": [],
  "renames": [],
  "nearDuplicates": [],
  "skippedInstances": [],
  "conflicts": []
}
```

`figmaPageNodeId` 只用于定位目标页面，不参与命名或去重签名。默认不向用户展示该字段；仅在同名页面、定位冲突或用户明确要求技术明细时展示。

## 默认预览

严格按以下结构输出摘要、四列表格、必要的实例跳过汇总和结束语。每个建议名称一行；完全重复组不展开成员。

```text
扫描到 26 个候选文本，建议使用 16 个名称。
其中 2 组完全重复共用名称，2 个同类字段分别命名，
1 个实例继承节点跳过。本次仅预览，未修改 Figma。

| 建议名称 | 节点数 | 中文含义 | 状态 |
| --- | ---: | --- | --- |
| coupon/countdown | 3 | 优惠券倒计时 | shared |
| reward/badge-rule | 1 | 徽章解锁条件 | rename |
| reward/gift-card-name | 9 | 礼物卡名称 | shared |
| chest/round | 1 | 当前轮次 | rename |
| chest/round-score | 1 | 当前轮次积分 | rename |
| ring/open-count-01 | 1 | 第一类戒指盒开启次数 | numbered |
| ring/open-count-02 | 1 | 第二类戒指盒开启次数 | numbered |

跳过：1 个实例继承节点，执行时不会直接修改。

回复“执行命名”后写入 Figma 并回读验证。
```

默认不显示节点标识、完整文本、样式代码、重复组成员、内部签名或逐项依据。只有 `confirm`、`failed`、需要解释的 `skip`，或用户明确要求明细时，才展开相关节点的当前名称、Figma 上下文和具体差异。

## 执行命名

仅在用户明确要求“执行命名”或同义指令后进入；跳过 `confirm` 和实例继承节点：

1. 用预览快照复核目标仍在范围内，文本、旧名称和实例状态未变化。
2. 按预览建议改名，不静默重新生成；工具有批量限制时每批最多 10 个节点，只改名称，不加载字体。
3. 每批后按节点标识回读。名称等于目标名称且仍通过规则校验才记为成功。
4. 分别报告成功、跳过、待确认、失败和未执行；回读不可用时标为 `executedUnverified`。写入失败后停止后续批次，不自动覆盖其他改动。

## 稳定性

- 再次扫描不得因折叠、隐藏、选中状态、扫描顺序或新增无关节点改变已有合法名称。
- 首次分配需要排序时可使用 Frame 页面顺序、层级/视觉顺序和节点标识；这些信息不得进入名称或去重签名。
