---
name: figma-text-naming
description: 识别 Figma 中含 `xx`、`XX` 等占位符的动态文字与特效文字，按 `文案/${business-domain}/${semantic-key}` 生成、复核和写回图层名称；用户同时要求上传 PC 时，将 Figma 文本样式转换为 HTML 后再上传。仅用于用户提供 Figma Design 链接并提到“动态文字命名”“特效文字命名”或“特效文本”时；其他措辞、单独的 Figma 链接以及一般设计查看、实现、修改或普通文字编辑请求不得触发。
---

# Figma 动态文本命名

## 执行流程

1. 调用 Figma `use_figma` 前，加载并遵循 `figma-use` Skill。
2. 完整读取 [references/dynamic-text-naming-rules.md](references/dynamic-text-naming-rules.md)。运行时不得读取飞书；该本地快照是唯一命名事实源。
3. 将用户链接限定的文件、页面或节点作为扫描范围。先定位页面容器和其直接子级的一级板块，再把文本节点归入唯一一级板块；不扩大扫描范围。
4. 读取文本节点的 `characters`，按规则快照和 [scripts/dynamic-text-naming.mjs](scripts/dynamic-text-naming.mjs) 的候选合同判断候选。不得用当前图层名称代替 `characters` 判断。
5. 对每个候选收集所属一级板块、板块业务域、字段语义和平行字段证据，检查当前名称及名称冲突。证据不足时直接标记 `confirm`，不猜测。
6. 用户同时要求上传 PC 时，先对全部候选读取 Figma styled text segments 并生成规范 HTML：按颜色、字号、字重、行高的固定顺序输出 style，字号按 `100px = 1rem` 转换，`X` / `XX` / `xx` 等占位符转换为 `{{}}`；多种样式生成多个连续 `<span>`。无法取得样式时将该项标记为 `confirm` 并跳过写入与上传。
7. 合并相同业务字段前比较第 6 步生成的完整 HTML。HTML 完全相同才能复用同一名称；HTML 不同则必须按规则快照使用稳定场景限定生成不同名称，不得选择其中一套样式覆盖，也不得用字号、节点 ID 或无语义数字区分。无法确定稳定场景限定时标记 `confirm`。
8. 对当前名称和拟写名称运行 `node '<本 Skill 目录>/scripts/validate-dynamic-text-name.mjs' '<name>'`。格式通过后仍须完成业务域、字段语义、名称冲突和 PC HTML 冲突检查。
9. 为每个文本节点确定且仅确定一个结果：`rename`、`keep`、`skip` 或 `confirm`。未要求上传 PC 时，相同业务字段仍按规则快照复用同一名称。要求上传 PC 时，在任何 Figma 或 PC 写入前建立完整的 `key -> HTML` 映射；同一 key 对应多个不同 HTML 时停止，把冲突项改为 `confirm`。
10. 用户明确要求只读、预览或不要修改时停止在结果预览。否则只把 `rename` 的名称写入对应文字图层 `node.name`，再回读每个已写节点确认实际名称；`keep`、`skip` 和 `confirm` 不写入。
11. 第 9 步检查通过后才上传所有 `rename` 和 `keep` 项。Page Center key 去掉图层名称的 `文案/` 前缀，`value` 必须使用生成的 HTML，不得直接上传 `characters`；逐项回读 key 与 HTML，回读不一致时如实报告。

## 读取合同

- 只接受 Figma Design 链接和用户自然语言要求作为任务输入。
- 只读取链接范围内完成结构归属和语义判断所需的 Figma 内容。
- 运行时不读取飞书、需求文档、代码仓库、外部配置或其他资料作为命名证据。
- 当前名称即使格式正确，也必须复核一级板块、业务域、字段语义和冲突；只有四项全部正确才能 `keep`。
- styled text segments 仅在用户要求上传 PC 时读取，用于生成 HTML 和判断同一名称能否安全复用，不得用于推断业务域或字段语义。

## 写入合同

- Figma Design 链接与“动态文字命名”“特效文字命名”或“特效文本”之一共同授权默认写回；用户明确要求只读、预览或不要修改时禁止写入。
- 只修改 `rename` 项对应文字图层的 `node.name`。
- 不修改候选文字的 `characters`、样式、位置、尺寸、可见性、一级板块结构、组件关系或其他 Figma 数据。
- 不得向同一个 Page Center key 写入两份不同 HTML；冲突检查必须发生在首次 Figma 或 PC 写入之前。
- Figma 连接器不可用、无编辑权限或写后回读不一致时，只报告实际状态，不声称写入成功。

## 输出合同

- 对候选输出动态文本、完整中文含义、结果和名称；“动态文本”必须逐字复制 `node.characters`，保留原语言、标点、大小写、空白、换行和占位符写法，不得翻译、概括、纠错或规范化；`confirm` 只给简短待确认原因。
- 相同业务字段可合并预览，但“动态文本”必须逐项列出组内每个不同的原文，不得用中文业务标签代替；写回结果必须覆盖组内每个可写文字节点。
- 上传 PC 时额外输出生成的 key、HTML value 和实际上传结果；样式冲突拆分名称时同时列出稳定场景限定，不得把纯文本报告为 HTML 上传成功。
- 除非用户要求，不输出内部账本、置信度、样式签名、节点 ID、统计或执行日志。
- 没有候选时用一句话说明。
