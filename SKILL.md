---
name: figma-text-naming
description: 扫描任意 Figma 文件、页面或节点范围，识别含 `xx`、`XX` 等占位符的动态文本，生成项目约定的业务 Key 和完整中文含义，并按需预览、命名或去重。用户消息中只要包含 Figma 链接就使用，无需同时说明扫描、命名或提供特定页面名、Frame 名；仅提供链接时默认只读预览，仅在用户明确要求命名或写回 Figma 时修改图层名称。
---

# Figma 特效文本命名

扫描 Figma 动态文本，生成稳定的业务 Key 和完整中文含义；只在用户明确授权时修改图层名称。

调用 Figma `use_figma` 前，必须先加载并遵循 `figma-use` Skill。

## 请求与权限

默认只读扫描，不修改 Figma。

- 用户消息中只要包含至少一个 Figma 链接，就直接按链接范围执行只读扫描；不要要求用户补充“扫描”“预览”或“命名”等命令。
- 用户要求预览、扫描或去重时，返回命名与分组结果。
- 只有用户明确要求命名或写回 Figma 时，才允许修改图层名称；修改前先返回建议，修改后回读验证。

## 执行顺序

1. 解析所有 Figma URL，并确定用户是否明确授权改名。
2. 按 [references/scanning.md](references/scanning.md) 根据链接限定扫描根节点，建立只读候选快照。
3. 按 [references/deduplication.md](references/deduplication.md) 比较规范化文本和样式并分组。
4. 按 [references/naming-rules.md](references/naming-rules.md) 复核或生成 Key。
5. 内部逐节点建立 `auditedTextNodes`，完成唯一分类和完整性闸门检查。
6. 按 Key 汇总返回简洁报告；仅展开异常、只读或用户明确要求的节点明细。
7. 仅在用户明确授权且完整性闸门通过时分批写回并验证。

## 引用路由

只读取当前阶段需要的文件，但在执行对应阶段前必须完整读取：

| 阶段       | 必读引用                                              | 负责内容                                      |
| ---------- | ----------------------------------------------------- | --------------------------------------------- |
| 定位与扫描 | [scanning.md](references/scanning.md)                 | URL 范围、扫描根节点选择、候选快照、实例处理  |
| 去重       | [deduplication.md](references/deduplication.md)       | 文本和样式签名、分组、无法比较                |
| 命名       | [naming-rules.md](references/naming-rules.md)         | Key、语义等价、分类、闸门和冲突               |

## 结果汇报

- 使用“内部逐节点、外部按 Key 汇总”的两层结构。内部账本保留每个节点的 `nodeId`、当前名称、完整原文、完整中文含义、去重组、语义等价组、建议名称、依据、置信度、校验和处理，用于完整性检查与写回。
- 默认返回候选节点数、精确去重组数、建议 Key 数，以及 `rename / keep / skip / confirm` 数量；主表使用 `建议名称 | Figma 文本 | 中文含义 | 节点数 | 处理`。
- 每个建议 Key 只显示一行，相同文本组不展开成员；有可靠一一对应证据的多语言源文与译文可以保持不同去重组并在同一 Key 行中列出文本版本。
- `处理` 只汇总该 Key 下非零的 `rename / keep / skip / confirm` 数量。占位符统一显示为 `{{}}`，换行显示为字面量 `\n`；表格中的长文本可以截断，内部完整原文和中文含义不得截断。
- 待确认、跳过和失败项使用 `页面名 > 扫描根节点 > 精简父级路径 > “文本摘要”` 定位，并说明原因。路径只使用用户可见且有语义的图层名。
- 同一可见路径存在多个相同文本时，先补充相对扫描根节点的人类方位；仍无法区分且边界数据可靠时，再补充四舍五入的根节点内相对坐标。
- 完整性闸门全部为零时只说明“全部通过”；存在非零项时逐项列出名称、原因、用户可读位置和 `nodeId`。
- 仅在存在 `confirm`、实例继承、只读、校验失败、同组混合处理、写回结果，或用户明确要求详细报告时展开相关节点。正常重复成员不展开。
- `nodeId` 用于内部回读、精确写回、异常定位和回滚，不得作为面向用户的唯一定位信息。默认不输出比较签名、置信度、逐项推理或完整样式代码。
- 只读预览或审核必须明确说明未写回 Figma。

## Figma 写回

仅在用户明确要求命名或写回 Figma 时执行：

1. 完整性闸门全部通过后才进入写回。
2. 写入前按 `nodeId` 复核节点仍在扫描范围内，且文本、旧名称、实例和只读状态与快照一致。
3. 按已展示的建议改名，不得静默重新生成名称；只写 `rename`，跳过 `keep / confirm / skip`。
4. 每批最多修改 10 个节点，且只修改图层名称，不修改文字内容、位置、尺寸、样式或组件关系。
5. 任一节点快照过期或任一调用、回读失败时停止后续批次，不覆盖人工修改，也不假设未验证的写入成功。
6. 写回后回读全部 `auditedTextNodes`，重新检查格式、语义、分组、样式编号和冲突，不只检查已修改节点。
7. 汇报成功、跳过和失败数量，并提供已成功项的 `nodeId: old -> new` 回滚清单；不得自动回滚。

## 核心约束

- 默认只读；没有明确改名授权时不得修改 Figma。
- 只处理本次链接选中的扫描根节点；链接已经限定节点时，不自动扩展到父级、同级或同页其他节点。
- 当前名称合法的候选仍参与语义复核和去重。
- 任何证据不足、数据冲突或无法可靠判断的项标记为 `confirm`，不得猜测。
