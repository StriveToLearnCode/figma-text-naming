---
name: figma-text-naming
description: 扫描 Figma 中名称为 `翻译页面[pagecenterPageId]` 的页面，在顶层 `预览图/*` Frame 内识别含 `xx`/`XX` 占位符的动态文本，按公司 `Page_Module_txtNN` 规范生成可读 Key，并跟踪独立 UUID 稳定身份，通过整批重验、失败回滚和回读校验保证 Figma 改名的原子性。用户要求扫描、预览、命名、写回或去重此类 Figma 文本时使用；默认仅预览，仅在用户明确要求时原子写回 Figma。
---

# Figma 文本命名

在封闭范围内扫描、去重并命名动态文本图层。默认保持只读；将预览、确认和 Figma 原子写入视为相互独立的阶段。

## 检查依赖

本 Skill 依赖 `figma-use`。调用 Figma `use_figma` 前，必须完整加载并遵循可用的 `figma-use` Skill。

若该 Skill 不可用或加载失败，或当前环境缺少只读扫描、节点重命名、按 ID 回读中的任一能力，则停止执行并明确报告缺失能力。不得猜测工具参数或改用未经说明的写入方式。

本 Skill 不读取或写入 Pagecenter，不执行任何 `pc` 命令。`pagecenterPageId` 仅用作页面作用域标识；生成的 `items` 仅作为只读的命名和去重产物交给下游流程。Figma 图层名只承载可读 Key，不承载 UUID；UUID 必须来自能够持久化该身份的权威配置或下游流程。

## 限定范围

1. 使用 `^翻译页面\[([^\]]+)\]$` 匹配 Figma 页面。
2. 原样提取方括号内的值作为 `pagecenterPageId`。不得将 Figma 文件键、Figma 页面节点 ID 或 Frame 节点 ID 当作 Pagecenter 页面 ID。
3. 只处理匹配页面中名称符合 `^预览图\/[^/]+$` 的直接子 Frame。
4. 检查这些 Frame 内的所有后代 `TEXT` 节点，包括隐藏图层。
5. 使用 [references/naming-rules.md](references/naming-rules.md) 中的表达式识别候选项和有效可读 Key。候选项表达式是本 Skill 的扫描规则，不是公司通用命名规范。
6. 将满足以下任一条件的节点视为实例继承文本，放入 `skippedInstances` 且不得尝试写入：
   - 位于 `INSTANCE` 后代中且不是可独立编辑的 override；
   - 修改名称会影响主组件或无法独立持久化；
   - Figma 工具将其标记为 `inherited`、`readonly` 或 instance child。

## 执行工作流

### 1. 发现并快照

- 解析 Figma URL，以只读方式定位目标页面和 Frame。无法唯一确定 `pagecenterPageId` 时停止。
- 为每个候选项记录节点 ID、当前名称、`characters`、语义祖先、相邻文本、可见性、实例状态、有意义的样式片段及所在 Frame。
- 在执行过程中记录 Figma 文件键、页面节点 ID 和按页面顺序排列的目标 Frame ID，用于范围校验。无需将这些内部标识放入返回结果，且不得与 `pagecenterPageId` 混用。

### 2. 构建预览计划

完整阅读 [references/naming-rules.md](references/naming-rules.md)、[references/text-key-catalog.md](references/text-key-catalog.md) 和 [references/deduplication.md](references/deduplication.md)，再在内部生成以下计划结构。该结构用于完整性校验和后续写回，不得直接作为用户报告逐条展开：

```json
{
  "pageId": "...",
  "renames": [
    {
      "nodeId": "...",
      "currentName": "...",
      "stableId": "UUID or null",
      "targetKey": "tab1_bg1_txt01"
    }
  ],
  "items": [{"id": "UUID", "key": "tab1_bg1_txt01", "runtimeKey": "text/tab1_bg1_txt01", "value": "...", "describe": ""}],
  "confirm": [],
  "skippedInstances": [],
  "conflicts": []
}
```

`renames` 只放入置信度高、可以直接执行的改名项。`stableId` 只接受权威来源中已经持久化的 UUID；不得使用 Figma 节点 ID 代替，也不得在只读扫描时生成一个下次无法复用的新 UUID。正常项不重复返回 `characters`、`confidence` 和 `reason`。仅当节点确实属于完全重复组时，才添加可选的 `duplicateGroup`。

将低置信度名称和近似重复歧义放入 `confirm`，并在其中提供相关 `nodeId`、`currentName`、`characters`、占位符中文语义及顺序、歧义类型、具体原因和供用户回答的问题。`unknown` 只表示证据不足，不得将多个 `unknown` 当作相同语义。有充分证据时提供 2 至 3 个互斥候选，将推荐项放在首位并说明理由；证据不足时直接询问业务含义，不得为了凑候选而虚构键名。将未解决的键冲突放入 `conflicts`，同时为每个冲突生成一个要求用户选择保留、拆分或提供自定义键的问题。Figma 文件、页面和 Frame 标识仅用于内部范围校验，不放入预览结构。

`items` 是交给下游流程的只读文本条目，不等同于本次改名记录，也不得触发 Pagecenter 读写。预览时，它必须包含计划改名且校验通过的有效节点、已经使用正确可读 Key 的节点，以及完全重复组去重后的唯一条目；写入后，移除未成功且未恢复为有效 Key 的节点。不得包含 `confirm`、`failed`、无法确认业务含义或占位符语义顺序的节点、配置来源不确定且会影响去重判断的节点、无法确认最终身份、Key 和值的实例继承文本或尚未解决键冲突的节点。缺少持久化 UUID 时，不得虚构 `items.id`；改为在 `confirm` 中标记 `missingStableId`，把 UUID 创建交给有持久化能力的下游流程。每个 `items.id` 只出现一次；同一个 `items.key` 不得映射到多个 UUID、值或业务语义。`runtimeKey` 必须等于 `text/` 加可读 Key，供代码侧 `$tf()` 使用。

### 3. 校验完整计划

开始任何写入前一次性完成全部校验：

- 确认每个节点仍在处理范围内，且 `skippedInstances` 中的节点未出现在 `renames`。
- 确认每个 `targetKey` 符合 `Page_Module_txtNN` 格式；已有 `stableId` 时确认它是有效 UUID。不同渲染内容或不同业务语义不得共享稳定身份或可读 Key。
- 确认完全重复组的 `renderSignature`、`semanticSignature` 和 `placeholderSemanticSequence` 全部一致并共享同一组稳定身份与可读 Key；任一关键语义仍为 `unknown` 且没有其他明确证据时，按近似重复处理，不得自动合并。
- 在内部将所有候选项分类为可改名、无需修改、待确认、实例继承或冲突，确保没有遗漏未处理项；无需逐项输出校验正常的节点。
- 若存在低置信度名称、近似重复歧义或未解决冲突，则不得写入；返回精简预览报告后立即向用户提出对应的命名问题。

只有用户明确要求命名、写回或执行已确认计划时才修改 Figma；否则内部保留完整计划，只按第 7 步返回精简预览报告。

### 4. 询问并吸收命名决定

对 `confirm` 和 `conflicts` 中的每个独立决策点提问。一次问题可以覆盖一个重复组或冲突组，但不得把不同业务语义的节点捆绑成一个决定。问题必须包含：

- 相关节点及足以辨认它们的文本或上下文；
- 需要用户决定的是业务含义、共用一组 UUID 与可读 Key 还是分别命名，或冲突身份如何拆分；
- 有证据支持的候选键、推荐项及理由；
- 用户可以直接提供自定义业务含义或符合格式的可读 Key；UUID 只能从权威配置取得。

用户回答后，将决定绑定到对应节点或组，重建整个预览计划并重新执行第 3 步。不得只替换展示文本，也不得把回答套用到未提及的相似节点。用户提供的可读 Key 仍须通过格式、语义一致性、去重和冲突校验；用户提供 UUID 时还须验证格式及其权威来源。不合规时说明原因并继续询问，不得擅自修正后写入。

只要仍有未解决项，就继续保持只读并仅返回更新后的精简预览报告和剩余问题。全部解决后，返回不含待确认项和未解决冲突的新版精简报告，等待用户确认写入。对命名问题的回答只表示解决命名决策，不等同于写入授权；仅当用户在同一条消息中明确要求按新版计划执行时，才可继续写入。

### 5. 重验已确认计划

用户确认预览后，执行最近一次已确认的计划，不得静默重新生成键名。

写入前按 ID 重新读取全部相关节点，并与生成预览时的内部快照比较，确认节点仍存在、`currentName` 和 `characters` 未变化、实例状态未变化且节点仍在原范围内。任一条件不满足时，不得执行旧计划；返回差异并重新生成预览供用户确认。

### 6. 原子写入 Figma 并回读

仅修改节点名称时无需加载字体。将本次全部 `renames` 视为一个不可拆分的逻辑事务；每次 `use_figma` 调用最多包含 10 个逻辑改名操作只是工具调用限制，不得把不同批次报告为独立成功。

开始第一批前，按稳定顺序重新读取全部目标节点并保存 `nodeId`、`oldName` 和 `targetKey` 快照。每批执行前再次确认该批节点仍等于快照中的 `oldName`，执行后立即回读并记录已成功修改的节点。全部批次完成后，再统一回读所有目标节点；只有每个名称都等于 `targetKey` 且符合格式，整个事务才算成功。

任一写入失败、节点丢失、批前状态漂移、批后回读不一致或最终统一回读不一致时，立即停止并回滚本事务此前全部成功修改：

1. 按实际写入顺序的逆序处理所有已修改节点；当前名称仍为 `targetKey` 时恢复为 `oldName`，已经是 `oldName` 时记为已恢复，出现其他名称时不得覆盖并记为 `restoreFailed`。
2. 按 ID 回读全部回滚目标，只有名称等于 `oldName` 才记为 `restored`。
3. 将本轮整体标记为失败，分别报告 `restored`、`restoreFailed` 和 `notExecuted`；不得保留或接受部分成功。
4. `restoreFailed` 为空时说明事务已完整恢复，设计稿未保留本轮修改；否则明确说明原子性未能恢复、设计稿仍处于部分修改状态，并列出需人工处理的节点。

### 7. 汇报结果

全部候选节点仍需在内部校验，但结果只返回汇总、异常项和回滚清单，不逐条展示正常节点。执行完成后使用以下简洁结构汇报：

```json
{
  "pageId": "...",
  "success": 0,
  "skip": 0,
  "confirm": 0,
  "failed": 0,
  "rollback": ["12:34  当前进度 -> tab1_progress_txt01"]
}
```

汇总数字按候选节点计数，且每个候选节点只计入一个主状态：

- `success`：本轮写回并回读成功，且事务结束后仍保留新名称的节点数。预览阶段或事务完整回滚后为 0。
- `skip`：本轮未修改且不需要用户决策的节点数，包括名称有效且无需修改的节点，以及因实例继承等原因无法修改的节点。后者仍须在异常项中展开。
- `confirm`：仍需用户确认业务语义、候选键、重复关系或冲突处理的节点数。
- `failed`：尝试处理但失败，或回滚后仍未恢复到预期状态的节点数。不得把仅因未获得写回授权而未执行的节点算作失败。

先返回汇总 JSON，再仅在存在内容时按以下用户可读标题展开：`需要确认`、`处理失败`、`实例内无法修改`、`其他不合规`、`回滚清单`。不要输出空标题、内部签名、置信度分数、工具调用细节或正常节点明细。

只展开 `confirm`、`failed`、无法修改的 `skippedInstances`，以及其他不合规节点。每条异常至少提供 `nodeId`、`currentName`、`suggestedKey` 和具体、可行动的 `reason`；只有理解歧义确有必要时才补充 `characters`。有多个合理键时，将 `suggestedKey` 写成按推荐顺序排列的候选；证据不足时明确写“待补充业务含义后生成”，不得虚构键名。无需展开名称有效且无需修改的 `skip` 节点。

对所有 `skip` 和 `confirm` 节点执行合规检查。显式列出仍为 `切图/*`、不符合可读 Key 正则或与其他渲染内容或业务语义冲突的名称；只要存在此类名称，就不得报告“全部命名合规”。`切图/*` 对切图仍是合法命名，只是在动态文本图层上不合规。

仅为最终保留成功写回状态的节点生成 `nodeId  oldName -> newName` 回滚清单。汇总 JSON 中 `rollback` 始终为数组；事务已经完整回滚或没有成功写回时使用空数组，并在用户可读结果中写“回滚：无回滚项”。不得把已经恢复旧名称的节点放入回滚清单。

仅在确有内容时补充 `items`、完全重复组、近似重复项、未解决冲突和事务恢复结果。使用简短中文说明其业务含义，不重复倾倒内部节点快照。例如，将恢复结果写成“事务已完整恢复，设计稿未保留本轮修改”或“事务未完整恢复，以下节点仍需人工处理”。若异常列表非空，结论只说明已发现的问题和下一步，不得使用“全部命名合规”等绝对表述。

## 稳定性要求

- 所有节点结果依次按 Frame 在页面中的顺序、节点在 Frame 内的层级顺序、节点 ID 排序。
- 完全重复组、近似重复组和冲突组按首个成员出现顺序排序；`items` 按 `key` 升序排序。
- 首次成功写入后，再次扫描必须产生 0 个 `renamed`，且可读 Key、UUID 和 `items` 与首次结果一致；不得因扫描顺序变化重排已有 `txtNN`。
- 不得因节点已经使用规范名称而生成替代键，也不得在 `items` 中重复输出相同文本条目。
