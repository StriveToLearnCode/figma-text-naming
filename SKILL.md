---
name: figma-text-naming
description: 扫描名称形如 `翻译页面[pagecenterPageId]` 的 Figma 页面，在顶层 `预览图/*` Frame 内识别含 `xx`、`XX` 等占位符的动态文本，生成项目约定的业务 Key、完整中文含义和保留颜色、字号、字重及换行的富文本，并按需预览、命名、去重或产出下游文本条目。用户要求扫描、预览、命名、去重、生成、导出、交付或上传特效文本时使用；默认仅预览，仅在用户明确要求命名或写回 Figma 时修改图层名称。本 Skill 不读写 Pagecenter。
---

---

# Figma 特效文本命名与交付

扫描 Figma 动态文本，生成业务 Key、富文本值和完整中文描述，并在用户明确要求时修改图层名称。

调用 Figma `use_figma` 前，必须加载并遵循 `figma-use` Skill。

本 Skill 不执行 `pc` 命令，不读取、比较、写入或发布 Pagecenter 配置。用户要求上传或同步时，本 Skill 只生成下游交付合同，不执行外部写入。

## 解释请求

- `扫描`、`预览`、`看看`：只读扫描并返回命名建议，不修改 Figma。
- `去重`：对本次 Figma 候选进行文本和样式分组，不读取外部历史数据。
- `命名`、`执行命名`、`写回 Figma`：按照预览建议修改 Figma 图层名称并回读验证。
- `生成特效文本`、`导出特效文本`、`交付文本`：生成完整下游交付合同，不修改外部系统。
- `上传特效文本`、`上传 Pagecenter`、`同步文本`、`sync`：生成 Figma 扫描结果和下游交付合同，并明确说明未执行外部写入。
- 只有用户同时明确要求 `命名` 或 `写回 Figma` 时，生成或上传文本的请求才会修改 Figma。

## 扫描范围

1. 使用以下表达式匹配目标 Figma 页面：

   ```regex
   ^翻译页面\[([^\]]+)\]$
   ```

2. 将括号中的原始内容作为 `pagecenterPageId`。

3. 不得将 Figma file key、页面节点 ID、Frame ID 或其他节点标识当作 `pagecenterPageId`。

4. 只扫描匹配页面的直接子级中，名称符合以下表达式的 `FRAME`：

   ```regex
   ^预览图\/[^/]+$
   ```

5. 扫描目标 Frame 内的全部后代 `TEXT`，包括隐藏节点。

6. 按 [references/naming-rules.md](references/naming-rules.md) 识别包含动态占位符的候选，不匹配 `1x` 等普通数量后缀。

7. 当前名称已经合法的候选仍须参与语义复核、去重和交付，不得直接跳过。

8. 实例继承或只读文本记录到 `skippedInstances`，保留主组件上下文，但不直接改名，也不进入下游合同。

### 页面 ID 异常

无法从页面名称唯一取得 `pagecenterPageId` 时：

- 可以返回只读扫描预览；
- 不得生成声称可直接写入的下游合同。

同一 Figma 文件中存在多个匹配页面，且提取出的 `pagecenterPageId` 相同时：

- 分别完成只读扫描；
- 不得跨页面自动合并；
- 不得生成可直接写入的下游合同；
- 将页面 ID 冲突标记为 `confirm`。

多个页面分别对应不同 `pagecenterPageId` 时，按页面独立扫描、命名和生成合同。

## 工作流

### 1. 发现候选与建立快照

解析 Figma URL，以只读方式定位目标页面和目标 Frame。

为每个候选记录：

```json
{
  "nodeId": "",
  "currentName": "",
  "characters": "",
  "chineseNote": "",
  "parentPath": "",
  "adjacentText": [],
  "visible": true,
  "instanceState": "",
  "frameName": "",
  "styledTextSegments": []
}
```

其中：

- `chineseNote` 只记录能够与候选唯一对应的中文备注；
- `parentPath` 只保留有助于判断业务语义的精简路径；
- `adjacentText` 只用于解决实际命名歧义；
- `styledTextSegments` 用于生成富文本和比较样式。

读取 styled text segments 是构建富文本的必需步骤。可以在首次扫描时读取，也可以按批次补查，但生成 `value` 前不得省略。

### 2. 构建候选文本条目

对每个非实例候选构建：

```json
{
  "nodeId": "",
  "key": "",
  "sourceText": "",
  "plainText": "",
  "value": "",
  "describe": "",
  "status": "ready"
}
```

`status` 只允许：

```text
ready
confirm
```

#### `sourceText`

保留 Figma 原始 `characters`，不得改写。

#### `plainText`

将动态占位符统一转换为 `{{}}`，同时保留：

- 占位符数量；
- 占位符位置和顺序；
- 完整文字；
- 标点；
- 空格；
- 换行。

#### `value`

根据 Figma 返回的 styled text segments 生成富文本值。

无需要保留的局部样式时：

1. 对完整 `plainText` 做 HTML 安全转义；
2. 将所有换行统一替换为字面量 `</br>`。

存在需要保留的局部样式时：

- 生成最小数量的 `<span>`；
- 保留颜色、字号、字重和样式作用范围；
- 保留完整文字和全部占位符；
- 连续且样式完全相同的片段可以合并；
- 不同样式片段不得合并；
- 不得补充、推测或发明 Figma 未返回的样式。

以下换行全部统一为字面量 `</br>`：

- CRLF；
- CR；
- LF；
- U+2028；
- U+2029。

不得在 `value` 中保留真实换行，也不得改写为：

```html
<br />
<br />
```

生成样式时：

- 先转义 `&`、`<`、`>` 和引号；
- RGB 每个颜色通道转为两位小写十六进制；
- 字号按 `px / 100` 转为 `rem`，最多保留两位小数；
- 字重按以下规则映射：

| Figma 字重  | 数值 |
| ----------- | ---: |
| Thin        |  100 |
| Extra Light |  200 |
| Light       |  300 |
| Regular     |  400 |
| Medium      |  500 |
| Semi Bold   |  600 |
| Bold        |  700 |
| Extra Bold  |  800 |
| Black       |  900 |

#### `describe`

忠实翻译完整 `plainText`：

- 占位符保持为 `{{}}`；
- 数量、状态、时间、条件和否定关系不得省略；
- 不得只写“礼品名称”“条件文案”等摘要；
- 不得根据业务上下文补充原文不存在的信息；
- 所有换行统一写成字面量 `</br>`。

#### 完整性校验

将 `value` 中的 HTML 标签和实体还原后，其文本投影必须等于 `plainText`。

校验前：

1. 将 `value` 中每个字面量 `</br>` 还原为 `\n`；
2. 将 `plainText` 中的 CRLF、CR、U+2028 和 U+2029 统一为 `\n`。

出现以下任意情况时，标记为 `confirm`：

- 文本缺失或增加；
- 占位符数量、位置或顺序变化；
- 样式片段无法可靠还原；
- 中文无法完整翻译；
- 文本使用渐变、图片填充或多个同时生效的文本填充；
- 颜色透明度无法按照下游格式可靠表达；
- 字重名称无法映射；
- styled text segments 缺失、重叠、顺序异常或无法覆盖全部字符；
- 存在项目当前不支持但会影响最终展示的文字样式。

`confirm` 项不得进入下游合同。

### 3. 去重与命名

执行前完整读取：

- [references/deduplication.md](references/deduplication.md)
- [references/naming-rules.md](references/naming-rules.md)
- [references/approved-name-corpus.md](references/approved-name-corpus.md)

不得读取或引用已经删除的文本 Key 词典。

#### 去重

去重只比较当前目标 Figma 页面内的候选。

判断原则：

```ts
const isSame =
  textSignatureA === textSignatureB && styleSignatureA === styleSignatureB;
```

- 规范化文字和样式都相同：视为同一组，共享一个 Key，只生成一个 `textItems` 条目。
- 文字或样式任意一项不同：视为不同组。
- 样式数据不足或无法可靠比较：标记为 `confirm`。

同一页面中的多个 `预览图/*` Frame 可以共同去重，但不得跨 Figma 页面自动分组。

Frame、组件、节点位置、158/388 等区域标识和当前图层名称不得改变去重结果。

#### 名称选择

按照 [references/naming-rules.md](references/naming-rules.md) 选择名称：

1. 当前名称合法、语义准确、用途一致且不存在冲突时，保持当前名称。
2. 当前名称不可用时，优先原样复用业务域、字段语义和用途都匹配的已批准先例。
3. 没有完整先例时，可以参考同页已经确认且语义一致的名称。
4. 仍无可用名称时，生成全小写 kebab-case 新名称。
5. Figma 证据不足或相互冲突时，标记为 `confirm`。

不得仅因为批准语料中存在另一个近义名称，就替换当前合法名称。

#### 相同文字、不同样式

文字相同但样式不同的候选分别命名。

只有这些候选具有相同文字和共同业务语义时，才可以在共同基础名称后使用两位样式编号：

```text
ring/box-open-count-01
ring/box-open-count-02
```

编号只区分样式版本，不表示：

- Frame；
- 158 或 388 等区域；
- 组件位置；
- 视觉位置；
- 节点顺序。

已有合法编号与样式版本稳定对应时，保留原编号。

新增样式版本使用组内未占用的最小两位编号。

整组首次命名且不存在已有编号时，按规范化样式签名排序；签名相同时，再使用 Frame 页面顺序和节点 ID 裁决。

`text2`、`chip1Short`、`cong1` 等已批准名称中的数字属于业务名称，不是样式编号。

### 4. 返回结果

#### 扫描、预览或去重

返回：

- 总候选数量；
- 建议名称数量；
- 三列表格；
- 确有内容时的待确认项。

不展示下游合同。

```text
扫描到 26 个候选文本，建议使用 15 个名称。
本次仅预览，未修改 Figma。

| 建议名称 | Figma 文本 | 中文含义 |
| --- | --- | --- |
| lottery/can-lottery-times | 当前可抽奖次数：{{}} | 当前可抽奖次数：{{}} |
| reward/name | بطاقة الهدية\n{{}} | 礼品卡\n{{}} |
| ring/proposal | أرسل 30 هدية من “{{}}” ... | 赠送 30 份来自“{{}}”的礼物…… |

待确认：
- `12:34`：样式片段缺失，无法生成完整富文本。
```

表格规则：

- 每个建议 Key 只显示一行；
- 相同文本组不展开成员；
- Figma 文本中的占位符统一显示为 `{{}}`；
- 换行显示为字面量 `\n`；
- 过长文本可以在表格中截断；
- 表格中的中文含义可以截断；
- 内部 `describe` 和交付 `value` 不得截断。

默认不输出：

- 内部比较签名；
- 置信度；
- 逐 Key 节点列表；
- 逐 Key 节点数量；
- 逐项判断过程；
- 完整样式代码。

#### 命名或写回 Figma

返回同一三列表格，并补充：

- 改名成功数量；
- 跳过数量；
- 待确认数量；
- 失败数量。

用户未要求生成文本时，不展示下游合同。

#### 生成、导出、交付或上传文本

返回：

- 简短摘要；
- 交付条目数量；
- 完整下游合同；
- 确有内容时的异常项。

用户要求上传或同步时，必须明确说明：

```text
已生成下游交付合同，未读取或写入 Pagecenter。
```

下游合同属于用户明确要求的交付结果，不属于禁止展示的内部 JSON。

### 5. 执行 Figma 改名

仅在用户明确要求命名或写回 Figma 时执行。

要求生成、导出、交付或上传文本，不代表允许修改 Figma。

执行时：

1. 使用预览快照复核节点仍在扫描范围内。
2. 复核文本、旧名称和实例状态未发生变化。
3. 按已经展示的预览建议改名，不得静默重新生成名称。
4. 每批最多修改 10 个节点。
5. 只修改图层名称，不加载字体，不修改文字内容或样式。
6. 每批完成后按节点 ID 回读。
7. 只有回读名称与目标名称一致，且仍通过命名规则校验时，才记为成功。
8. 某批发生写入失败时，停止执行后续批次。
9. 跳过 `confirm`、实例继承和只读节点。

用户同时要求改名和生成合同的情况下：

- 改名失败的节点不得进入合同；
- 未确认 Key 的节点不得进入合同；
- 已成功改名且状态为 `ready` 的节点才可以进入合同。

### 6. 下游交付合同

用户要求生成、导出、交付或上传特效文本时，按页面生成：

```json
{
  "pagecenterPageId": "",
  "textItems": [
    {
      "key": "reward/name",
      "value": "<span style=\"...\">...</span>",
      "describe": "完整中文翻译"
    }
  ]
}
```

合同规则：

- `textItems` 只包含 `ready` 项；
- 规范化文字和样式都相同的候选组只出现一个条目；
- 文字或样式不同的候选分别出现；
- 每项只包含 `key`、`value` 和 `describe`；
- 不包含节点 ID、Figma 路径、内部签名、状态或写入操作；
- `value` 必须包含完整原文、全部占位符和项目支持的局部样式；
- `describe` 必须是完整中文翻译；
- `value` 和 `describe` 中占位符的数量与顺序必须一致；
- `confirm`、实例继承、Key 冲突和不满足改名条件的失败项不得进入合同；
- 多个有效页面分别生成独立合同。

合同生成后，本 Skill 即结束。

任何 Pagecenter 读取、差异比较、写入、发布或写后验证都由下游写入方负责。

## 稳定性

- 再次扫描不得因 Frame 折叠、节点隐藏、选中状态、扫描顺序或新增无关节点改变已有合法名称。
- 仅 Frame、组件、业务区域或视觉位置变化时，不得改变去重结果。
- 已有样式编号与对应样式版本稳定匹配时，不得重新编号。
- 新增样式版本使用组内未占用的最小两位编号。
- 文本、占位符结构、有效局部样式或可观察业务语义发生变化时，重新复核去重和名称。
- 下游合同是纯数据产物，不表示任何外部写入已经发生。
