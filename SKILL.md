---
name: figma-text-naming
description: 扫描名称形如 `翻译页面[pagecenterPageId]` 的 Figma 页面，在直接子级 `预览图/*` Frame 中发现、语义命名并去重动态文本；默认输出精简预览，用户明确要求执行命名或回滚时才写入 Figma。
---

# Figma 动态文本命名

只负责 Figma 动态文本的扫描、语义命名、去重、改名、回读和显式回滚。默认只读；用户明确要求“执行命名”或同义指令后才改名。

## 职责边界

- 不加载、查询或调用 Figma 之外的系统，也不生成任何下游配置或写入入口。
- 页面名方括号内的 `pagecenterPageId` 仅用于识别目标页面和匹配本地固定兼容键。名称含该值不得触发任何外部能力。
- 固定兼容键完全按 [references/text-key-catalog.md](references/text-key-catalog.md) 的本地规则判断。

## 按阶段加载 Figma 规则

保留对 `figma-use` 的强制依赖。调用 `use_figma` 前按宿主要求加载其主 Skill；不要因此加载其他 Figma Skill、全部引用文档或完整 API 类型文件。加载器支持章节定位时只读当前阶段的相关章节；不支持时读主流程，再只按方法名检索当前操作直接需要的引用和 API 声明。

1. **只读扫描**：只加载 URL 解析、页面切换、页面/节点查找、名称/类型/文本/层级/可见性/实例状态读取，以及分页、返回大小和截断处理规则。需要时再检索少量样式读取方法。
2. **执行命名**：只有用户明确要求执行命名时，额外加载节点名称修改、每批操作限制、返回变更节点 ID 和按节点 ID 回读的规则，并检查写入与回读能力。
3. **回滚**：只有用户明确要求回滚时，额外加载恢复节点名称所需的写入、状态复核和回读规则。

后续阶段能力缺失不得阻止前一阶段。不得猜测工具参数或使用未经说明的写入方式；能力以实际可用工具为准。

## 扫描范围

1. 用 `^翻译页面\[([^\]]+)\]$` 匹配页面，原样提取方括号内容作为 `pagecenterPageId`。
2. 只进入该页面名称匹配 `^预览图\/[^/]+$` 的直接子级 Frame。
3. 扫描这些 Frame 内的所有后代 `TEXT` 节点，包括隐藏节点。
4. 按 [references/naming-rules.md](references/naming-rules.md) 的精确表达式识别含 `xx`、`XX` 等占位符的候选项。
5. 实例继承或只读文本仍列入结果，但标为 `skip`，不得写入。

## 两阶段扫描

### 第一阶段：候选发现

在单个已定位页面中，把页面/Frame 定位和候选扫描合并为一次只读查询。每个候选只返回：

```json
{
  "nodeId": "",
  "name": "",
  "characters": "",
  "parentPath": [],
  "frameId": "",
  "frameName": "",
  "visible": true,
  "instanceState": "editable | inherited | unknown"
}
```

`parentPath` 只包含从目标 Frame 到节点的精简名称路径，不返回祖先属性。第一阶段禁止返回完整 fills、完整文本样式、全部祖先属性、组件对象、布局属性、图片或导出信息。

在发起查询前估算结果大小并设置安全页大小；候选较多时用稳定游标分页，每页仍只返回上述字段。不得先请求超大结果、等待截断，再缩小重跑。分页结果按 Frame 页面顺序、节点层级/视觉顺序和 `nodeId` 合并。

读取 [references/naming-rules.md](references/naming-rules.md) 完成高置信度命名；只有需要比较复用关系时才读 [references/deduplication.md](references/deduplication.md)，只有需要选择业务词时才读 [references/text-key-catalog.md](references/text-key-catalog.md)。先在本地完成文本规范化、初步分组、合法名称复核和高置信度建议。

以下证据足以直接进入预览，不触发补查：有意义的现有中文名称；`parentPath` 已明确对象；候选文本自身含义明确；命中本地固定兼容键；多个节点的文本、路径和用途明显一致。

### 第二阶段：一次批量补查

汇总全部存在实际歧义的节点，只发起一次批量上下文查询。每个节点只返回解决其疑问需要的字段，可以包含直接语义祖先、相邻文本、组件名、变体名，以及必要的局部颜色、字号或字重差异。不得重新返回第一阶段全部候选或高度重叠的多份上下文。命名依据只允许来自 Figma 中直接可观察的信息：所属 Frame 或组件的语义名称、相邻标签、写回前图层名称、组件或变体状态、文本内容与占位符结构。

仅以下情况触发补查：

- 业务域无法从第一阶段证据确认；
- 相同文本可能代表不同对象；
- 需要组件或变体状态才能判断用途；
- 需要区分同类编号或稳定业务标识；
- 存在可能改变含义的样式差异；
- 可能属于实例继承文本；
- 当前名称与可见上下文互相冲突。

样式不是第一阶段字段。只有两个节点的文本和可观察语义相同、需要确认能否完全复用；文本含多段样式且差异可能影响名称复用；或用户明确要求查看样式差异时，才读取这些节点的局部文本样式。判断只记录“相同”或“有意义差异”；默认不输出颜色代码、完整属性或序列化标记。颜色差异只能辅助已经存在的语义判断，不能单独决定业务域、字段含义或编号。样式差异不自动生成长 Key，同类字段优先使用共同短语义加稳定编号。

第二阶段后仍缺少 Figma 正向证据的节点直接标为 `confirm`，不得持续补查。两个戒指盒只补查相关节点即可；按词典规则用可观察的 `158`、`388` 稳定标识区分，不读取其他候选。徽章、礼物卡等对象在 Figma 证据足够时正常命名，证据不足时才进入 `confirm`。

## 分类与命名

- `exact`：文本结构、Figma 语义、用途和必要样式一致，共用一个无编号 Key。
- `near`：文本相同或相近，但语义、状态、所属对象或有意义样式不同；分别命名或使用稳定编号。
- `unique`：没有重复或近似候选，独立命名。
- `confirm`：完成一次补查后，Figma 证据仍不足或互相冲突。

格式固定为 `<business-domain>/<semantic-key>`，路径段使用小写英文 kebab-case。语义键只表达核心业务含义，一般使用 1 至 3 个英文词；只有删除词语会导致同一业务域内的字段无法区分时才增加词。优先使用无编号 Key；编号和固定兼容键优先级以引用文档为准。当前名称只有在格式、语义和复用关系均正确时才保留。

## 预览输出

默认结果严格按以下顺序输出：两句摘要、四列表格、必要的实例跳过汇总、唯一结束语。表格每个建议名称一行；完全重复组只显示节点数量和 `shared`，不列成员。固定兼容 Key 当前名称已经正确时使用 `skip`，不得计划再次改名。

```text
扫描到 26 个候选文本，建议使用 16 个名称。
其中 2 组完全重复共用名称，2 个戒指盒开启次数字段分别命名，
1 个实例继承节点跳过。本次仅预览，未修改 Figma。

| 建议名称 | 节点数 | 中文含义 | 状态 |
| --- | ---: | --- | --- |
| coupon/countdown | 3 | 优惠券倒计时 | shared |
| reward/badge-rule | 1 | 徽章解锁条件 | rename |
| reward/gift-card-name | 9 | 礼物卡名称 | shared |
| chest/round | 1 | 当前轮次 | rename |
| chest/round-score | 1 | 当前轮次积分 | rename |
| txt/lottery | 1 | 当前抽奖次数 | skip |
| reward/outfit-name | 1 | 服饰名称 | rename |
| txt/had-send | 1 | 已发送次数 | skip |
| txt/times | 1 | 奖励倍数 | skip |
| txt/treasury | 1 | 当前财富值 | skip |
| txt/progress | 1 | 当前能量值 | skip |
| ring/proposal | 1 | 求婚条件 | rename |
| ring/open-count-388 | 1 | 388 戒指盒开启次数 | numbered |
| ring/open-count-158 | 1 | 158 戒指盒开启次数 | numbered |
| member/renew-chance | 1 | 续费获得的开箱机会 | rename |
| member/open-chance | 1 | 当前开箱机会 | rename |

跳过：1 个实例继承节点，执行时不会直接修改。

回复“执行命名”后写入 Figma 并回读验证。
```

默认表格不显示转换后的语言值、完整文本、节点 ID、颜色代码、重复组成员、实例源节点 ID、内部签名或逐项命名依据，也不解释高置信度项。只有 `confirm`、`failed`、需要说明无法写入原因的 `skip`，或用户明确要求查看明细时，才在表格后展开相关节点 ID、当前名称、Figma 上下文和具体差异；只展开相关项，不附带正常项明细。默认结束语必须逐字使用：`回复“执行命名”后写入 Figma 并回读验证。`

## 执行命名

仅在用户明确要求执行命名后进入。无需再次确认，跳过 `confirm` 和实例继承节点：

1. 加载写入与回读的最少规则并检查能力；只能读取时保留预览，写入状态记为 `unavailable`。
2. 用第一阶段快照复核目标仍在范围内，文本、旧名称和实例状态未变化，并记录 `nodeId`、`oldName`、`targetName`。
3. 按刚构建的建议改名，不静默重新生成。工具要求限制时每批最多 10 个节点；只改名称不加载字体。
4. 每批后按该批节点 ID 回读。只有名称等于 `targetName` 且仍通过规则校验时记为成功；缺少回读能力时记为 `executedUnverified`。
5. 分别记录成功、跳过、待确认、失败、未执行和未验证节点。失败后停止后续写入，不自动加载或执行回滚。

## 显式回滚

只有用户明确要求回滚时，才加载回滚规则并使用最近一次写入记录。仅当节点当前名称仍等于本轮 `targetName` 时恢复 `oldName`；出现其他名称时不得覆盖。按节点 ID 回读并分别报告已恢复、恢复失败和未处理数量。

## 查询预算与稳定性

- 同一单页面预览通常使用 1 次精简扫描；存在低置信度节点时增加 1 次批量补查，预计共 2 次 Figma 只读查询。
- 执行命名时，在上述只读查询后按每批最多 10 个节点追加写入与对应的按 ID 回读；回滚查询不预加载、不计入普通预览。
- 再次扫描不得因折叠、隐藏、选中状态、扫描顺序或新增无关节点改变已有合法名称。
- 不把节点 ID、文件键、页面 ID、位置、随机值或自动容器名编码进名称。
