---
name: figma-text-naming
description: 扫描 Figma 翻译页面中的动态文本图层，判断现有图层名是否符合 `Page_Module_txtNN` 规范，基于 Figma 内可见语义生成稳定名称，并按用户请求预览或直接改名、回读验证。用户要求发现、检查、预览、命名、写回或整理 Figma 动态文本图层时使用；只负责 Figma 命名，不生成或同步任何下游消费数据。
---

# Figma 文本命名

只处理 Figma 动态文本图层的发现、合法性判断、稳定命名、预览、改名和回读验证。扫描或预览请求保持只读；用户明确要求命名、写回或执行时，在同一轮扫描后直接处理可确定的改名项，不要求再次确认写入。

## 职责边界

本 Skill 的职责止于 Figma 图层命名。不得读取、生成、推断或写入 Pagecenter items、HTML value、Vue `$tf()`、`runtimeKey`、配置数据来源、`pc` 命令或 UUID 映射。调用方已经提供某节点的 UUID 时，可以把它作为可选元数据原样附在该节点结果中；不得主动查找、创建、补全、校验来源或持久化 UUID。UUID 是否存在不得影响候选发现、合法性判断、命名、复用、去重、`confirm` 或 Figma 写入。需要创建或持久化 UUID 映射时，交给负责下游映射的其他 Skill。

调用 Figma `use_figma` 前，完整加载并遵循可用的 `figma-use` Skill。

## 分阶段检查能力

只检查当前阶段实际需要的能力，不得用后续阶段的能力缺失阻断前一阶段：

1. **扫描预览**：只要求读取页面、节点、文本和基础样式。即使当前环境不能重命名节点或不能按节点 ID 回读，也必须完成扫描并返回预览。
2. **执行改名**：仅在用户明确要求命名、写回或执行时进入；除扫描能力外，额外要求节点重命名能力。缺少该能力时不写入，但仍返回扫描预览，并把写入状态标记为 `unavailable`。
3. **验证结果**：写入后仅在环境支持按节点 ID 回读时执行。缺少该能力不得反过来阻止扫描或改名；应把已发出的改名标记为 `executedUnverified`，明确说明无法确认最终名称，不得声称“已验证成功”或“原子完成”。

不得猜测工具参数或改用未经说明的写入方式。能力判断应基于实际可用工具，不得只因能力未预先声明就推定不可用。

## 限定 Figma 范围

1. 使用 `^翻译页面\[([^\]]+)\]$` 匹配 Figma 页面。
2. 将方括号内容仅作为不透明的 `scopeLabel`，用于区分本次扫描结果；不解释、不验证其外部含义。
3. 只处理匹配页面中名称符合 `^预览图\/[^/]+$` 的直接子 Frame。
4. 检查目标 Frame 内的所有后代 `TEXT` 节点，包括隐藏图层。
5. 使用 [references/naming-rules.md](references/naming-rules.md) 中的精确表达式识别动态文本候选项和合法图层名。
6. 位于 `INSTANCE` 后代、修改名称会影响主组件或被 Figma 标记为只读/继承的文本放入 `skippedInstances`，不得写入。

## 工作流

### 1. 发现与快照

- 只读定位目标页面、Frame 和候选文本。
- 为每个候选记录 `nodeId`、当前名称、`characters`、Frame、语义祖先、相邻文本、可见性、实例状态和有意义的样式差异。
- 记录页面节点 ID、目标 Frame ID 和节点顺序，仅用于范围复核、稳定排序和回读；不得写入生成的名称。

### 2. 判断现有名称

完整阅读 [references/naming-rules.md](references/naming-rules.md)。需要判断多个节点能否共享名称时，再阅读 [references/deduplication.md](references/deduplication.md)；需要解释业务语义时，再阅读 [references/text-key-catalog.md](references/text-key-catalog.md)。

将每个候选归入且只归入以下状态之一：

- `valid`：当前名称格式合法、语义范围一致，且没有名称冲突；无需修改。
- `rename`：证据充分，可以生成唯一稳定名称。
- `confirm`：Figma 内证据不足以确定页面、模块或业务含义，或可观察证据存在冲突并支持多个合理解释，仍需用户决定。去重不得因后端接口、Pagecenter 配置、Vue 变量或代码绑定不可见而进入 `confirm`。
- `skippedInstance`：实例继承或只读，无法安全改名。

合法正则只是最低条件。格式正确但 Page、Module 与上下文明显不一致，或同名节点语义冲突时，仍不得判为 `valid`。

### 3. 生成稳定名称

- 名称格式固定为 `Page_Module_txtNN`，写入值仅包含该可读名称。
- 优先保留语义一致的已有合法名称；不得因扫描顺序、图层移动或新增节点重命名已有合法项。
- 新名称的 Page 和 Module 只从 Figma 内证据与用户确认中选择。
- 保留同一 `Page_Module` 下已有编号；新增项使用下一可用 `txtNN`。首次分配按 Frame 顺序和节点视觉顺序稳定排序，节点 ID 仅作最终裁决。
- 只有满足 [references/deduplication.md](references/deduplication.md) 的同名条件时，多个节点才可共享名称。没有足够的 Figma 正向证据时分别命名；只有 Figma 证据本身存在待裁决歧义时才进入 `confirm`。
- 禁止把 Figma 节点 ID、文件键、页面节点 ID、Frame 序号或随机值编码进名称。

### 4. 构建预览并解释写入意图

扫描后构建以下精简预览：

```json
{
  "scopeLabel": "...",
  "summary": {"valid": 0, "rename": 0, "confirm": 0, "skippedInstance": 0},
  "renames": [
    {"nodeId": "12:34", "currentName": "数字", "targetName": "tab1_progress_txt01", "uuid": "已有时原样附带，否则省略"}
  ],
  "confirm": [],
  "skippedInstances": []
}
```

`renames` 只包含高置信度项。调用方已提供 UUID 时，在对应节点上原样附带可选的 `uuid`；没有 UUID 时省略该字段，不得因此加入 `confirm`。`confirm` 必须包含相关节点、可辨认的 Figma 上下文、相互冲突或支持多个解释的具体证据，以及用户需要回答的问题；有充分证据时提供 2 至 3 个互斥候选并将推荐项放在首位，命名语义确实无法从 Figma 判断时直接询问业务含义。不得要求用户补充后端接口字段、Pagecenter 配置来源、Vue 运行时变量或代码数据绑定来完成去重。

若用户只要求扫描或预览，返回预览后结束，不写入。若用户已经明确要求命名、写回或执行，则该请求本身就是写入授权：完成扫描和计划校验后直接改名，不得为了展示预览而暂停并索取第二次确认。`confirm` 中存在歧义时只跳过相关节点；先执行其余可确定项。用户补充命名决定后，沿用原写入意图处理这些节点，无需再问一次是否写入。

### 5. 执行改名

仅在用户已表达写入意图后检查节点重命名能力。能力缺失时返回现有预览和 `writeStatus: "unavailable"`，不得丢弃或隐去扫描结果。

执行刚构建的 `renames`，不得静默重新生成名称。写入前使用扫描阶段已有的读取能力复核目标仍在原处理范围内、当前名称和文本未变化、实例状态未变化；任一条件变化时跳过受影响节点并报告差异。仅改名称时无需加载字体。工具要求分批时，每批最多 10 个改名操作。

### 6. 按能力验证与恢复

1. 第一批写入前保存全部目标的 `nodeId`、`oldName` 和 `targetName`。
2. 支持按节点 ID 回读时，每批写入后立即回读，全部批次完成后再统一回读所有目标。只有名称等于 `targetName` 且格式合法的节点才记为 `renamedVerified`。
3. 支持按节点 ID 回读时，任一写入失败或回读不一致都停止后续写入，并按实际写入顺序逆序恢复已经修改的名称。仅当当前名称仍为本轮 `targetName` 时恢复 `oldName`；出现第三方名称时不得覆盖。回读回滚目标并分别报告 `restored`、`restoreFailed` 和 `notExecuted`。
4. 不支持按节点 ID 回读时，不执行依赖回读的原子性断言或恢复判断。根据工具返回记录已发出的操作，并将其报告为 `executedUnverified`；若写入调用报错则停止后续批次，分别报告工具确认已接收、明确失败和未执行的节点。

### 7. 汇报

先给出汇总，再只展开异常、确认项和最终保留的改名结果。每个候选只计入一个主状态。区分 `renamedVerified` 与 `executedUnverified`，并报告 `writeStatus` 和 `verifyStatus`。只有回读确认最终保留新名称的节点才能进入确定的 `nodeId oldName -> targetName` 回滚清单；未验证写入单独列出，不得混入已验证成功项。

不得输出下游消费结构。结果中可以包含调用方已提供的可选 UUID，但它只用于结果关联。除此之外可以包含：

- 合法且未修改的数量；
- 成功改名并回读通过的节点；
- 已发出改名但因缺少按 ID 回读能力而未验证的节点；
- 仍需确认的命名问题；
- 无法修改的实例节点；
- 失败、恢复和未执行节点；
- 最终回滚清单。

## 稳定性要求

- 按 Frame 页面顺序、节点层级/视觉顺序、节点 ID 排序结果。
- 首次成功写入后再次扫描必须产生 0 个改名项。
- 不得因扫描顺序、Frame 折叠、隐藏、选中状态或新增无关节点改变已有合法名称。
- 不得为格式合法且语义一致的节点生成替代名称。
- 任何写入都必须来自用户当前或仍有效的明确写入意图；用户已经要求写入时不得再索取第二次确认。
- 缺少写入或回读能力时仍须返回扫描预览；未回读的写入不得记为已验证成功。
